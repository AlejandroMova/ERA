{
    "engines": {
      "node": "16.0.0", 
      "npm": "7.10.0"
    },
  "scripts": {
    "build:digitalocean": "npm install --production=false && npm run build && npm ci", 
    "start": "node ./bin/www"
  },
  "dependencies": {
    "dotenv": "^16.0.3",
    "express": "^4.18.2"
  }
}
